# build / generated output
derivedData/
Comment Summarizer/
*.xcodeproj/
xcuserdata/
DerivedData/

# OS clutter
.DS_Store

# secrets — never commit API keys
.env
.env.*
*.local
doxa-amo.env

# built release artifact (attached to GitHub Releases, not committed)
doxa-extension-*.zip
web-ext-artifacts/
doxa-*.xpi
.amo-upload-uuid

# local work-state memo (not part of the deliverable)
PROGRESS.md
