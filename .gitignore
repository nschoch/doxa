# build / generated output
derivedData/
DerivedData/
xcuserdata/
*.pbxproj.bak

# OS clutter
.DS_Store

# secrets — never commit API keys
.env
.env.*
*.local
doxa-amo.env
*.p8

# built release artifact (attached to GitHub Releases, not committed)
doxa-extension-*.zip
web-ext-artifacts/
doxa-*.xpi
.amo-upload-uuid

# local work-state memo (not part of the deliverable)
PROGRESS.md
APPSTORE.md

# App Store screenshots — local staging (contains personal browsing session)
appstore-screenshots/
